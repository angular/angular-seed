'use strict';

describe('hereiam.events module', function() {

  beforeEach(module('hereiam.events'));

  describe('events controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var eventsController = $controller('EventsController');
      expect(eventsController).toBeDefined();


      
    }));

  });
});