'use strict';

describe('hereiam.people module', function() {

  beforeEach(module('hereiam.people'));

  describe('people controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var peopleController = $controller('PeopleController');
      expect(peopleController).toBeDefined();
    }));

  });
});